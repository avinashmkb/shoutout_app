<%= form_for(@feedback_topic) do |f| %>
  <% if @feedback_topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@feedback_topic.errors.count, "error") %> prohibited this feedback_topic from being saved:</h2>

      <ul>
      <% @feedback_topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :feedback_id %><br />
    <%= f.number_field :feedback_id %>
  </div>
  <div class="field">
    <%= f.label :trendingtopic_id %><br />
    <%= f.number_field :trendingtopic_id %>
  </div>
  <div class="field">
    <%= f.label :branch_id %><br />
    <%= f.number_field :branch_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
