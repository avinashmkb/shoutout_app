<%= form_for(@sentiment) do |f| %>
  <% if @sentiment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sentiment.errors.count, "error") %> prohibited this sentiment from being saved:</h2>

      <ul>
      <% @sentiment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :value %><br />
    <%= f.number_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
