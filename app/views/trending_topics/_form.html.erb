<%= form_for(@trending_topic) do |f| %>
  <% if @trending_topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trending_topic.errors.count, "error") %> prohibited this trending_topic from being saved:</h2>

      <ul>
      <% @trending_topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br />
    <%= f.text_field :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
